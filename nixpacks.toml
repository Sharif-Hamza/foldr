[variables]
  NODE_ENV = "production"

[phases.setup]
  nixPkgs = ["nodejs", "qpdf"]

[phases.install]
  cmds = [
    "npm install --production"
  ]

[phases.build]
  cmds = ["echo 'No build step required'"]

[start]
  cmd = "npm start" 